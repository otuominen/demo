apiVersion: v1
kind: Service
metadata:
  name: {{ .Release.Name }}-redis-follower
  labels:
    app: {{ .Release.Name }}-redis
    role: {{ .Release.Name }}-follower
    tier: {{ .Release.Name }}-backend
spec:
  ports:
  - port: 6379
  selector:
    app: {{ .Release.Name }}-redis
    role: {{ .Release.Name }}-follower
    tier: {{ .Release.Name }}-backend
